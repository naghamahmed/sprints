version: "3.9"
services:
  app:
    build: .
    ports:
      - "5000"
    depends_on:
      - db

  web:
    image: nginx
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "1111:80"
    depends_on:
      - app
  db:
    image: "mysql:latest"
    restart: always
    volumes:
      - mysql:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD:MY-PASS
volumes:
    mysql: {}
~
~
